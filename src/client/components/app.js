const React = require('react');
const Form = require('./form.js');
const Results = require('./results.js');

function App() {
  return (
    <div>
      <Form />
      <Results />
    </div>
  );
}

module.exports = App;
